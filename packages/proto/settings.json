{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SettingsSync",
  "description": "App settings sync (capability settings-sync). Last-writer-wins: the blob with the greater updated_unix wins; ties go to mac. clipboard_mode is off|mac_to_phone|phone_to_mac|two_way. Send immediately when paired, else on the next heartbeat/announce.",
  "type": "object",
  "additionalProperties": true,
  "required": ["protocol_v", "type", "sender", "capabilities", "payload"],
  "properties": {
    "protocol_v": { "type": "integer", "minimum": 1 },
    "type": { "type": "string", "enum": ["settings-sync"] },
    "sender": {
      "type": "object",
      "additionalProperties": true,
      "required": ["platform", "app_build", "min_peer_build"],
      "properties": {
        "platform": { "type": "string" },
        "app_build": { "type": "integer", "minimum": 0 },
        "min_peer_build": { "type": "integer", "minimum": 0 },
        "app_version": {
          "type": "string",
          "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+(-[0-9A-Za-z.-]+)?$",
          "description": "Human app version, e.g. 0.2.0. Display-only; older peers omit it."
        }
      }
    },
    "capabilities": {
      "type": "array",
      "items": { "type": "string" }
    },
    "payload": {
      "type": "object",
      "additionalProperties": true,
      "required": ["nonce", "clipboard_mode", "updated_unix"],
      "properties": {
        "nonce": {
          "type": "string",
          "minLength": 1,
          "description": "Random per-message value the ack must echo."
        },
        "clipboard_mode": {
          "type": "string",
          "enum": ["off", "mac_to_phone", "phone_to_mac", "two_way"]
        },
        "notifications_enabled": {
          "type": "boolean",
          "description": "Master switch for phone-to-Mac notification mirroring. Absent means true."
        },
        "updated_unix": {
          "type": "integer",
          "minimum": 0,
          "description": "Unix seconds of the last local change; greater wins."
        },
        "updated_by": {
          "type": "string",
          "enum": ["mac", "macos", "android"],
          "description": "Which side made the last change; ties go to mac."
        }
      }
    }
  }
}
